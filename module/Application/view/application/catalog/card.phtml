<?php
/**
 * @var Dashboard\Entity\File $file
 * @var Application\Entity\Vehicle $item
 * @var Dashboard\Entity\Taxonomy $option
 * @var Zend\View\Renderer\PhpRenderer $this
 */
?>
<div class="container">
    <div class="row">
        <div class="col-lg-offset-1 col-md-offset-1 col-lg-11 col-md-11 breadcrumbs">
            <a href="<?= $this->url('catalog', ['action' => 'index']);?>" class="back">
                <span></span>
                <?= $this->translate('Каталог');?>
            </a>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-lg-offset-1 col-md-offset-1 col-lg-11 col-md-11">
            <div class="auto-card-title">
                <?= $this->translate($item->name());?>
            </div>
            <div class="auto-card-price">
                <?= $item->price(',');?>
            </div>
                <div class="jcarousel-wrapper">
                <div class="jcarousel">
                    <ul>
                        <?php foreach ($item->getFiles() as $file) :?>
                        <li>
                            <img src="<?=$file->getPreview('487x350'); ?>" width="487" height="350" alt="<?= $item->name()?>"/>
                        </li>
                        <?php endforeach; ?>
                    </ul>
                </div>
                <a href="#" class="jcarousel-control-prev"></a>
                <a href="#" class="jcarousel-control-next"></a>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
    <div class="col-lg-offset-1 col-md-offset-1 col-lg-10 col-md-10 col-sm-12">
        <div class="auto-card-info-box">
            <div class="auto-card-info-title">
                <?= $this->translate("Технические характеристики");?>
            </div>
            <div class="row">
                <div class="col-lg-5 col-md-5 col-sm-5 auto-card-into-column">
                    <ul>
                        <li><span><?= $this->translate('Марка');?></span><?= $this->translate($item->mark());?></li>
                        <li><span><?= $this->translate('Модель');?></span><?= $this->translate($item->model());?></li>
                        <li><span><?= $this->translate('Модификация');?></span><?= $item->getName();?></li>
                        <li><span><?= $this->translate('Год');?></span><?= $item->getRegistrationDate();?></li>
                        <li><span><?= $this->translate('Пробег');?></span><?= $item->mileage();?></li>
                    </ul>
                </div>
                <div class="col-lg-5 col-md-5 col-sm-5 auto-card-into-column">
                    <ul>
                        <li><span><?= $this->translate('Тип двигателя');?></span><?= $this->translate($item->getFuel()->getName());?></li>
                        <li><span><?= $this->translate('Объем двигателя');?></span><?= $item->getVolume();?><?=$this->translate('л')?></li>
                        <li><span><?= $this->translate('КПП');?></span><?= $this->translate($item->getTransmission()->getName());?></li>
                        <li><span><?= $this->translate('Кузов');?></span><?= $this->translate($item->getBody()->getName());?></li>
                        <li><span><?= $this->translate('Привод');?></span><?= $this->translate($item->getDrive()->getName());?></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="auto-card-info-box">
            <div class="auto-card-info-title">
                <?= $this->translate("Комплектация");?>
            </div>
            <div class="row">
                <div class="col-lg-5 col-md-5 col-sm-5 auto-card-into-column">
                    <ul>
                        <?php $options = $item->getOptions(); ?>
                        <?php foreach ($options->slice(0, ceil($options->count()/2)) as $option) : ?>
                            <li><?= $this->translate($option->getName());?></li>
                        <?php endforeach; ?>
                    </ul>
                </div>
                <div class="col-lg-5 col-md-5 col-sm-5 auto-card-into-column">
                    <ul>
                        <?php foreach ($options->slice(ceil($options->count()/2)) as $option) : ?>
                            <li><?= $this->translate($option->getName());?></li>
                        <?php endforeach; ?>
                    </ul>
                </div>
            </div>
        </div>
        <div class="auto-card-info-box">
            <div class="auto-card-info-title">
                <?= $this->translate("Комплектация");?>
            </div>
            <div class="row">
                <div class="col-lg-5 col-md-5 col-sm-7 auto-card-into-text">
                    <?= $item->getDescription(); ?>
                </div>
            </div>
        </div>
    </div>
    </div>
</div>